# Safety Monitoring System — Environment Variables
# ===================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
#
# On CT104, place at:  /opt/safety-monitor/.env
# The app loads it automatically via python-dotenv.

# ── Home Assistant ─────────────────────────────────────────────────────────────
# Long-lived access token from HA → Profile → Long-Lived Access Tokens
HA_LONG_LIVED_TOKEN=your_ha_token_here

# ── Victron Venus OS (MQTT) ────────────────────────────────────────────────────
# CONFIRMED Feb 2026 (live probe of 192.168.2.132):
#   Device:    Victron GX running Venus OS
#   Battery:   SmartShunt 500A/50mV (instance 279)
#   Protocol:  MQTT on port 1883, no auth on LAN
#   Portal ID: found in Venus OS Remote Console → Settings → VRM Portal ID
#              OR from MQTT: subscribe to N/+/system/0/Serial
VICTRON_IP=192.168.2.132
VICTRON_MQTT_PORT=1883
VICTRON_PORTAL_ID=c0619ab88ee0
VICTRON_BATTERY_INSTANCE=279

# ── EG4 Inverter (SolarmanV5 data-logger) ─────────────────────────────────────
# CONFIRMED Feb 2026 (live probe of 192.168.2.49):
#   Protocol:      SolarmanV5 binary (port 8000) — NOT HTTP
#   Port 80:       CLOSED
#   Port 8000:     OPEN — raw TCP banner, 197 bytes on connect
#   Dongle serial: BJ43500603
#   Logger SN:     4372670006  (too large for pysolarmanv5 uint32)
#   Firmware:      FAAB-2122
#
# Local banner method (default — EG4_USE_CLOUD=false):
EG4_LOCAL_IP=192.168.2.49
EG4_PORT=8000
EG4_LOGGER_SN=4372670006
EG4_USE_CLOUD=false

# Cloud API method (set EG4_USE_CLOUD=true to use instead of local banner):
EG4_USERNAME=your_eg4_portal_email
EG4_PASSWORD=your_eg4_portal_password

# ── Pushover Notifications ─────────────────────────────────────────────────────
# Create app at https://pushover.net/apps/build
PUSHOVER_USER_KEY=your_pushover_user_key
PUSHOVER_API_TOKEN=your_pushover_app_token

# ── Google Drive (daily report upload) ────────────────────────────────────────
# Service account JSON key — download from Google Cloud Console
# IAM & Admin → Service Accounts → Keys → Add Key → JSON
GOOGLE_SERVICE_ACCOUNT_JSON=/opt/safety-monitor/service_account.json
# Drive folder ID from the URL: drive.google.com/drive/folders/<FOLDER_ID>
GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id

# ── SMS (optional, not yet implemented) ───────────────────────────────────────
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
# SMS_TO_NUMBERS=+15551234567,+15559876543
